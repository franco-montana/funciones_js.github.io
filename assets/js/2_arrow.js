const suma = (a, b) => a + b;

// la arrow function es una forma más consisa de escribir la función en JS. si la función tiene solo una linea y devueve un valor, se puede omitir el uso de las llaves y el return.
