const example = function(a,b,c) {
    return a + b + c;
}


// una función de expresión asigna a una función anónima (sin el nombre) a una variable. Esto nos permite reutilizar la función llamandola mediante el nombre de la variable.